{
  "name": "",
  "version": "0.0.0",
  "scripts": {
    "postinstall": "scripts/npm-client install && scripts/npm-server install",
    "postversion": "git push && git push --tags"
  },
  "devDependencies": {
    "chokidar": "^1.6.1",
    "gulp-livereload": "^3.8.1",
    "ido": "^2.0.0",
    "write": "^0.3.3"
  }
}
