{
  "name": "",
  "version": "0.0.0",
  "scripts": {
    "postinstall": "docker-compose run --rm npm-client && docker-compose run --rm npm-server",
    "postversion": "git push && git push --tags"
  },
  "devDependencies": {
    "chokidar": "^1.6.1",
    "gulp-livereload": "^3.8.1",
    "ido": "git+https://git@github.com/gomoto/ido.git#ab72e5045f084f156a42327eb52047345924bd5d",
    "write": "^0.3.3"
  }
}
